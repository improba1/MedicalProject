services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    networks:
      - app-network

  assistant:
    build: ./ai_assistant
    ports:
      - "8000:8000"
    depends_on:
      - backend
    env_file:
      - .env
    networks:
      - app-network

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
      - assistant
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
